# 🚐 مشروع كروة (Karwa) - المخطط الاستراتيجي الشامل

**المبرمج:** Error404 (NotFound_Dev) 💻 | **التاريخ:** 2026-02-06
**المكان:** واسط - العراق 🇮🇶 | **الثيم:** Dark Mode 🌑⚪

---

## 🛠️ 1. التقنيات والخدمات (The Stack)

### **الوصف والفائدة:**

اختيار هذه التقنيات يهدف لبناء تطبيق سريع، آمن، وقابل للتوسع. نستخدم نظام **MERN** لتوحيد لغة البرمجة (JavaScript) في كل المشروع، مما يسرع عملية التطوير.

- **Frontend (React.js):** لبناء واجهة مستخدم تفاعلية وسريعة الاستجابة.
- **Backend (Node.js & Express):** لمعالجة الطلبات البرمجية بسرعة عالية.
- **Database (MongoDB):** لتخزين البيانات بمرونة عالية (NoSQL).
- **Cloudinary:** لرفع صور المستمسكات (بطاقة وطنية، سكن) وحفظها سحابياً لضمان الأمان.
- **JWT:** لتأمين عمليات الدخول والحفاظ على خصوصية المستخدم.

---

## 📂 2. هيكل المجلدات (Project Structure)

### **الوصف والفائدة:**

الفائدة من هذا التقسيم هو تنظيم الكود بحيث يسهل الوصول لأي ملف وتعديله دون التأثير على بقية الأجزاء، وهو نظام احترافي يتبعه كبار المبرمجين.

- **/models:** يحتوي على "قوالب البيانات" (الجداول).
- **/controllers:** يحتوي على "عقل التطبيق" (المنطق البرمجي).
- **/routes:** يحتوي على الروابط التي تربط الواجهة بالسيرفر.
- **/middleware:** حراس البوابة (للتحقق من هوية المستخدم أو الأدمن).
- **/components:** الأجزاء البرمجية المتكررة (مثل الأزرار أو الـ Navbar).

```text
/karwa-app
├── /backend
│   ├── /config (db.js, cloudinary.js)         # إعدادات الاتصال بالخدمات الخارجية
│   ├── /models (User.js, Route.js)            # هيكلة البيانات (Schemas)
│   ├── /controllers (auth, admin, route)      # كتابة المنطق البرمجي لكل عملية
│   ├── /routes (authRoutes, adminRoutes)      # المسارات التي يستدعيها الفرونت اند
│   └── server.js                              # نقطة الانطلاق الرئيسية للسيرفر
├── /frontend
│   ├── /src
│   │   ├── /components (UI Elements)          # الأجزاء القابلة لإعادة الاستخدام
│   │   ├── /pages (Home, Login, Admin)        # الصفحات الكاملة للتطبيق
│   │   └── App.js                             # موزع المسارات الرئيسي (Router)
└── .env
```

---

## 🗄️ 3. قاعدة البيانات (Data Models)

### **الوصف والفائدة:**

تصميم الجداول هنا يركز على **الأمان والشمولية**. جدول المستخدمين يدعم رفع 4 صور ثبوتية، وجدول الخطوط يدعم نظام المحافظات للتوسع المستقبلي.

**أ- جدول المستخدم (User Schema):**

- `fullName`, `phone`, `password`, `role` (Admin, Driver, Passenger).
- `accountStatus`: (Pending, Approved, Banned, Rejected).
- `documents`: روابط صور (بطاقة وطنية وجه/ظهر، بطاقة سكن وجه/ظهر).

**ب- جدول الخطوط (Route Schema):**

- `province`: (المحافظة)، `fromArea`: (منطقة الانطلاق)، `toArea`: (الوجهة).
- `price`: (السعر)، `time`: (الوقت)، `availableSeats`: (المقاعد).

**ج- جدول الحجوزات (Booking Schema):**

- يربط الراكب بالخط المختار ويخزن حالة الحجز (مقبول/مرفوض).

---

## 👥 4. حالات الاستخدام (Use Cases)

### **الوصف والفائدة:**

تساعدنا في تخيل "رحلة المستخدم" داخل التطبيق من البداية للنهاية، وتضمن إننا ما نسينا أي صفحة أو زر مهم.

* **رحلة الراكب (Passenger Journey):**
  
  1. يفتح التطبيق -> يبحث عن (محافظة ومنطقة).
  
  2. تظهر له النتائج -> يختار الخط المناسب.
  
  3. يرسل "طلب اشتراك" -> ينتظر موافقة السائق.
  
  4. يستلم إشعار بالقبول -> يبدأ التواصل مع السائق.

* **رحلة السائق (Driver Journey):**
  
  1. ينشئ حساب -> يرفع صور المستمسكات الـ 4.
  
  2. ينتظر تفعيل الحساب من "الأدمن" (ما يگدر يضيف خط إذا حسابه Pending).
  
  3. بعد التفعيل -> يضيف تفاصيل الخط (السعر، المناطق، الوقت).
  
  4. يستقبل طلبات الركاب -> يسوي (Accept/Reject).
     
     ---

## 🎨 5. هندسة الواجهات (UI/UX)

### **الوصف والفائدة:**

تم تصميم الواجهات لتكون "بسيطة ومباشرة" لضمان سهولة الاستخدام حتى لغير المحترفين تقنياً.

1. **صفحة الهبوط (Home):** تحتوي على محرك بحث "ذكي" يبدأ باختيار المحافظة ثم المنطقة لتقليل نتائج البحث غير المرغوبة.
2. **صفحة التسجيل (Register):** للسائق، تحتوي على واجهة رفع ملفات (Upload UI) سهلة تسمح برفع صور الهويات بوضوح.
3. **لوحة الأدمن (Admin Panel):** واجهة مخصصة لمراجعة صور المستمسكات وتدقيقها والموافقة على السائقين.
4. **لوحة السائق (Driver Dashboard):** لإدارة طلبات الركاب ورؤية من يريد الاشتراك في خطه.

---

## 🗓️ 6. جدول التنفيذ (Roadmap - Speed Coding)

### **الوصف والفائدة:**

خطة عمل مكثفة لمدة 5 أيام، كل يوم يمثل مرحلة بناء واضحة تصلح لتصوير فيديوهات Speed Coding حماسية.

- [x] **اليوم 1 (التأسيس):** تنصيب السيرفر، ربط قاعدة البيانات، وإعداد Cloudinary لرفع الصور. 🏗️
- [x] **اليوم 2 (الأمان):** بناء نظام تسجيل السائق ورفع المستمسكات الـ 4 وتشفير البيانات. 🪪
- [ ] **اليوم 3 (الرقابة):** بناء لوحة تحكم الأدمن لتدقيق هويات السائقين وتفعيل حساباتهم. 👑
- [ ] **اليوم 4 (البحث):** برمجة محرك البحث الجغرافي (محافظات/مناطق) وعرض النتائج. 🌍
- [ ] **اليوم 5 (الختام):** ربط نظام الحجز بين الراكب والسائق وتنسيق الواجهات النهائية. 🏁

---

## 📝 7. قواعد التسمية (Standards)

### **الوصف والفائدة:**

استخدام معايير ثابتة في التسمية يجعل الكود "نظيفاً" (Clean Code) وسهل الفهم للمتابعين الذين يشاهدون فيديوهاتك.

- **Variables:** نستخدم `camelCase` (مثل: `isVerified`).

- **React Components:** نستخدم `PascalCase` (مثل: `RouteCard.jsx`).

- **Folders:** نستخدم `kebab-case` (مثل: `auth-middleware.js`).

- **Emojis:** نستخدم الإيموجي في الـ `console.log` لزيادة التفاعل في الفيديوهات. 🚀🔥
  
  ```
  
  ```
